Мини-регистраиця пользоваетля в браузере:
1. Запрошу имя пользователя через prompt > cохраню как string;
2. Запрошу возраст , но переведу его как число;
3. Запрошу соглашение у пользователя через confirm > boolean;
4. Создам переменную userId - как большое число > bogInt;
5. Создам переменную bonusCode, значение которой null;
6. Создам переменную secondName , которая будет со значением undefined:

После сбора данных:
- выведу alert с результатом;
- выведу в console.log() все переменные с их типами: